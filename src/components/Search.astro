---
import type { HTMLAttributes } from 'astro/types';
import { Icon } from 'astro-icon/components';

export interface Props extends HTMLAttributes<'div'> {
  placeholder?: string;
}

const {
  placeholder = "Rechercher...",
  ...props
} = Astro.props;
---

{/**
<div class="relative" {...props}>
  <form action="/recherche" method="get" class="flex items-center">
    <input
      type="text"
      name="q"
      placeholder={placeholder}
      class="w-full px-4 py-2 text-sm text-gray-900 bg-gray-50 rounded-lg border border-gray-300 focus:ring-primary-500 focus:border-primary-500 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white"
    />
    <button
      type="submit"
      class="absolute right-2 p-2 text-gray-600 hover:text-gray-900 dark:text-gray-400 dark:hover:text-white"
    >
      <Icon name="tabler:search" class="w-5 h-5" />
    </button>
  </form>
</div>
*/}

{/**
<div id="autocomplete"></div>
<script type="module" src="/autocomplete.js"></script>
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@algolia/autocomplete-theme-classic" />
*/} 